# Create .env.local with the following, server-side only:
COURTLISTENER_API_TOKEN=replace-with-your-token
MAX_INPUT_CHARS=64000

# CourtListener’s blog post specifies ~64,000 character max per request; we enforce it server-side.
# Free Law Project

